name: thermo_drift_env
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - torch
  - dash
  - matplotlib
  - Bio
  - torch.nn
  - torch.optim
  - torch.autograd 
prefix: /opt/anaconda3/envs/thermo_drift_env
