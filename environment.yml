name: thermo_drift_env
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - pytorch
  - dash
  - matplotlib
  - biopython
  - scikit-learn
  - ipdb
prefix: /opt/anaconda3/envs/thermo_drift_env
